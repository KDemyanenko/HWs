fn main() {
   let v = {
       let mut x = 1;
       x += 2; // Додав крапку з комою, щоб завершити вираз
       x // Явно повертаю x
   };

   assert_eq!(v, 3);

   println!("Success!");
}

---------------------------------------------------------------------------------------------------
fn main() {
   let v = {
       let x = 3; // Додав блок {}, щоб присвоїти x значення v
       x // Явно повертаю x
   };

   assert!(v == 3);

   println!("Success!");
}


---------------------------------------------------------------------------------------------------
fn main() {
    let s = sum(1, 2);
    assert_eq!(s, 3);

    println!("Success!");
}

fn sum(x: i32, y: i32) -> i32 {
    x + y // Видалив крапку з комою, щоб вираз став значенням, яке повертається з функції
}


